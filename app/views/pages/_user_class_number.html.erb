<div class="panel panel-default">
	<div class="panel-body">
		<%= form_for(@class_number_user) do |f| %>

      <div class="form-group">
        <%= f.label :class_number, class: "form-label" %><br />
        <%= f.email_field :class_number, autofocus: true, class: "form-control" %>
      </div>
      <%= f.submit "sign up", :class => "btn btn-primary" %>
     <% end %>

		<!-- need to figure out how to edit the following line properly -->
		<%= current_user.inspect %>
</div>
