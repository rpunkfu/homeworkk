<h1>Listing Groups</h1>


    <% @groups.each do |group| %>
	      <%= group.name %>
	      <%= group.end_time %>
        <%= link_to 'Show', group %>
        <%= link_to 'Edit', edit_group_path(group) %>
        <%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %>
        <br>
    <% end %>

    <br>

<%= link_to 'New Class Day', new_group_path %>
<br><br>